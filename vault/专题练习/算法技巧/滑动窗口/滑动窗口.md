---
date: 2025-07-06
aliases:
  - sliding window
tags:
  - 双指针
  - 算法技巧
  - 数组
---
## 概述

“**滑动窗口**（Sliding Window）是一种在数据序列上高效处理**线性连续子序列**的算法策略。其核心思想是将固定大小或动态调整的窗口在数据序列上滑动，通过**复用已有计算结果**来减少重复计算，从而优化时间复杂度。”

[滑动窗口可视化](https://algorithm-visualizer.org/dynamic-programming/sliding-window)

不难发现，滑动窗口实际上是由同向[[双指针]]来实现的，对于大小固定的窗口，左指针和右指针之间的距离就是固定的。
## 适用场景

### 连续子数组 / 子串问题：

- **固定窗口大小**：如求数组中长度为 `k` 的最大子数组和、字符串中是否存在某个排列。
- **动态窗口大小**：如最小覆盖子串、无重复字符的最长子串（通过调整窗口左右边界）。

### 越短越合法

一般要写 `ans += right - left + 1`。

内层循环结束后，`[left,right]` 这个子数组是满足题目要求的。由于子数组越短，越能满足题目要求，所以除了 `[left,right]`，还有 `[left+1,right],[left+2,right],…,[right,right]` 都是满足要求的。也就是说，当右端点固定在 right 时，左端点在 `left,left+1,left+2,…,right`的所有子数组都是满足要求的，这一共有 `right−left+1` 个。

### 越长越合法

一般要写 `ans += left`

内层循环结束后，`[left,right]` 这个子数组是不满足题目要求的，但在退出循环之前的最后一轮循环，`[left−1,right]` 是满足题目要求的。由于子数组越长，越能满足题目要求，所以除了 `[left−1,right]`，还有 `[left−2,right],[left−3,right],…,[0,right]` 都是满足要求的。也就是说，当右端点固定在 `right` 时，左端点在 `0,1,2,…,left−1` 的所有子数组都是满足要求的，这一共有 `left` 个。

要注意，滑动窗口的局限性体现在我们要处理的是**连续子序列**，例如“找到数组中的不连续子集”，或者“在无序数组中查找和为目标值的任意子数组”这样的问题就不好用滑动窗口

## 总结

通过维护一个窗口，在线性时间内解决子串/子数组问题

滑动窗口的优点在于，我能够将上一个窗口的数据**复用**到下一个窗口，**不用做重复的工作**，相当于用O(1)的时间复杂度干了O(n)的活。所以我只需要初始化一次窗口，接下来只需要更新数据就能一直用了。

“滑动窗口”实际上就是[[双指针]]的一个变体，就是一个用**双指针**来维护的[[队列]]
滑动窗口解决问题和核心是“滑动”（这是一种枚举吗？）

## 案例

[[删掉一个元素以后全为 1 的最长子数组]]
[[拆炸弹]]
[[爱生气的书店老板]]
[[可获得的最大点数]]
[[子数组最大平均数 Ⅰ]]
[[半径为k的子数组平均值]]
[[几乎唯一子数组的最大和]]
[[定长字串中最大元音字母数]]
[[删掉一个元素以后全为 1 的最长子数组]]
[[无限数组的最短子数组]]
[[将x减到0的最小操作数]]
[[考试的最大困扰度]]
[[删除子数组的最大得分]]
[[最短且字典序最小的美丽子字符串]]
[[替换子串得到平衡字符串]]
[[乘积小于 K 的子数组]]
[[包含所有三种字符的子字符串数目]]
[[必须拿起的最小连续卡牌数]]
[[和相同的二元子数组]]


