---
date: 2025-08-13
tags:
  - 哈希表
  - 枚举
  - 数组
  - 中等
---
[统计坏数对的数目](https://leetcode.cn/problems/count-number-of-bad-pairs/)
## 解题思路

- [[枚举右，维护左]]：这道题跟[[好数对的数目]]没有本质上的区别，只要稍微变式，查找`nums[i] - i == nums[j] - j`的数量，再用总数减去它就行了

```cpp
class Solution {
public:
    long long countBadPairs(vector<int>& nums) {
        unordered_map<int, int> map;
        int n = nums.size();
        long long ans = (long long)n * (n - 1) / 2;
        for (int i = 0; i < n; i++) {
            ans -= map[nums[i] - i]++;
        }
        return ans;
    }
};
```

## 复杂度分析

### 时间复杂度：**O(n)**

### 空间复杂度：**O(n)**

## 新知识点

## 总结

这里还用到了[[逆向思维]]