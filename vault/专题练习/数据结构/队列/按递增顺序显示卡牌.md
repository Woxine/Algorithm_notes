---
date: 2025-09-10
tags:
  - 暴力模拟
  - 队列
  - 排序
  - 中等
---
[按递增顺序显示卡牌](https://leetcode.cn/problems/reveal-cards-in-increasing-order/)
## 解题思路

排序+双端[[队列]]+[[逆向思维]]+模拟：这道题最后要求的是“经过一系列操作之后能够符合条件的”**初始值**，我们已经知道最后是什么样子了，于是倒着模拟就能得到答案。也就是先将牌组底部的牌移到顶部；然后再往牌堆顶部加一张新牌。这种涉及在首位增删操作的情况就能用双端[[队列]]

```cpp
class Solution {
public:
    vector<int> deckRevealedIncreasing(vector<int>& deck) {
	    // 先排序，得到最终状态，再反推
        sort(deck.begin(), deck.end());
        deque<int> dq;
        for (int i = deck.size() - 1; i >= 0; i--) {
	        // 将牌组底部的牌移到顶部
            if (!dq.empty()) {
	            dq.push_front(dq.back());
                dq.pop_back();
            }
            // 加入新牌
            dq.push_front(deck[i]);
        }
        return vector<int>(dq.begin(), dq.end());
    }
};
```
## 复杂度分析

### 时间复杂度：**O(n)**

### 空间复杂度：**O(n)**

## 新知识点

## 总结

