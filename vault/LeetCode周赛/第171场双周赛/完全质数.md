---
date: 2025-12-09
tags:
  - 数学
  - 枚举
  - 中等
---
[3765. 完全质数 - 力扣（LeetCode）](https://leetcode.cn/problems/complete-prime-number/description/)
## 题目拆解 / 题意分析

## 解题思路

纯模拟

## 代码实现

```cpp
class Solution {
public:
    bool isPrime(int n) {
        for (int i = 2; i * i <= n; i++) {
            if (n % i == 0)
                return false;
        }
        return n >= 2;
    }
    bool completePrime(int num) {
        string s = to_string(num);
        int len = s.size();
        int temp = 0, mul = 10;
        // 前缀
        for (int i = 0; i < len; i++) {
            temp = temp * mul + (s[i] - '0');
            if (!isPrime(temp)) {
                return false;
            }
        }
        temp = 0;
        mul = 1;
        // 后缀
        for (int i = len - 1; i >= 0; i--) {
            temp += (s[i] - '0') * mul;
            mul *= 10;
            if (!isPrime(temp)) {
                return false;
            }
        }
        return true;
    }
};
```

STL中string库函数优化：

```cpp
class Solution {
    bool is_prime(int n) {
        for (int i = 2; i * i <= n; i++) {
            if (n % i == 0) {
                return false;
            }
        }
        return n >= 2;
    }

public:
    bool completePrime(int num) {
        string s = to_string(num);
        for (int i = 0; i < s.size(); i++) {
            // 前缀
            int x = stoi(s.substr(0, i + 1));
            if (!is_prime(x)) {
                return false;
            }
            // 后缀
            x = stoi(s.substr(i));
            if (!is_prime(x)) {
                return false;
            }
        }
        return true;
    }
};
```
## 复杂度分析

### 时间复杂度：**O($\sqrt{num}$​)**

### 空间复杂度：**O(log num)**

## 总结 / 错误反思

对STL库还是不熟啊，比赛的时候还是手打的

## 知识补充

`stoi(string)`：把字符串变成整数
`substr(pos,len)`：截取从 pos 开始，长度为 len 的子串